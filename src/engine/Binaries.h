#pragma once
#pragma warning(disable:4200)
#include <windows.h>
#include <list>

#include "Structures.h"
#include "DisassemblyReader.h"

using namespace std;
using namespace stdext;

class Binaries
{
public:
    Binaries();
    ~Binaries();
};
